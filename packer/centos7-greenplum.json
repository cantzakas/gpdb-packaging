{
  "variables": {
    "ssh_name": "configuser",
    "ssh_pass": "CHANGEME",
    "hostname": "gpdbox"
  },
  "builders": [
    {
      "type": "virtualbox-ovf",
      "source_path": "{{user `base_os`}}",

      "output_directory": "build/centos7-greenplum",

      "ssh_username": "{{user `ssh_name`}}",
      "ssh_password": "{{user `ssh_pass`}}",
      "ssh_wait_timeout": "30s",

      "shutdown_command": "sudo /sbin/halt -h -p",

      "boot_wait": "8s",

      "format": "ova",
      "vm_name": "GPDB-{{user `gp_version`}}",
      "export_opts": [
        "--manifest",
        "--vsys", "0",
        "--description", "CentOS 7 running a single node Greenplum {{user `gp_version`}} database.",
        "--version", "{{user `gp_version`}}"
      ],
      "vboxmanage": [
        [ "modifyvm", "{{.Name}}", "--cpus", "2" ],
        [ "modifyvm", "{{.Name}}", "--memory", "8192" ],
        [ "modifyvm", "{{.Name}}", "--vram", "24" ],
        [ "modifyvm", "{{.Name}}", "--natpf1", "ssh-forwarding,tcp,,2222,,22" ],
        [ "modifyvm", "{{.Name}}", "--natpf1", "sql-forwarding,tcp,,5432,,5432" ]
      ]
    }
  ],
  "provisioners": [
    {
      "type": "file",
      "source": "{{user `greenplum_zip`}}",
      "destination": "~/greenplum.zip"
    },
    {
      "type": "shell",
      "execute_command": "/bin/bash '{{ .Path }}'",
      "script": "packer/resources/centos7-greenplum.sh"
    }
  ]
}
